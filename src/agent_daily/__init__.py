"""Agent Daily — 运行在 macOS (Apple Silicon) 上的个人 AI Agent 系统。

Phase 0: 项目初始化与接口冻结。本阶段仅建立项目骨架、冻结模块接口，
并提供 `agent-daily doctor` 环境自检；不实现 Agent 逻辑 / MLX 调用 /
GitHub 采集 / 飞书 API / Scheduler。
"""

__version__ = "0.1.0"
